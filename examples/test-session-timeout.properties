# Test configuration for session timeout functionality
# This file demonstrates the new session timeout configuration options

# Basic session timeout configuration
operaton.webapp.session-cookie.timeout-in-seconds=900
operaton.webapp.session-cookie.warning-time-in-seconds=120

# Existing session cookie settings (unchanged)
operaton.webapp.session-cookie.enable-secure-cookie=false
operaton.webapp.session-cookie.enable-same-site-cookie=true
operaton.webapp.session-cookie.same-site-cookie-option=strict

# Other webapp settings (for context)
operaton.webapp.application-path=/operaton
operaton.webapp.index-redirect-enabled=true